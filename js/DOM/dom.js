//DOM:- DOCUMENT OBJECT MODEL
//DOM selectors
// to select id use #
// to select class use .
//to select input use input[]
//for selecting one item we use query selector
//for multiple we use query selector all:
   //this does not contain an actual array s 
   // its does not have the prototype of map
// as we get a html collection it is also not an Array
// so we can't perform for each function on it
// first dtore it in an Array then apply functions
// it is done by Array.from(name of htmlcollectionf)
document.getElementById('title')